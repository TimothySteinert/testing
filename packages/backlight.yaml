switch:
  - platform: template
    name: "Backlight"
    id: backlight_switch
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - lambda: |-
          id(build_backlight_msg)(true);   // true = ON
    turn_off_action:
      - lambda: |-
          id(build_backlight_msg)(false);  // false = OFF

script:
  - id: build_backlight_msg
    mode: restart
    parameters:
      state: bool
    then:
      - lambda: |-
          // Clear buffer
          for (int i = 0; i < 16; i++) id(uart_msg)[i] = 0xFF;

          // Message ID
          id(uart_msg)[0] = 0xB1;

          // Byte 1 = Backlight state
          id(uart_msg)[1] = state ? 0x01 : 0x00;

          // Byte 2 = Reserved (Night)
          id(uart_msg)[2] = 0x00;

          // Byte 3 = Reserved (Custom)
          id(uart_msg)[3] = 0x00;

          // Byte 4 = Brightness (fixed 50%)
          id(uart_msg)[4] = 50;

          // Bytes 5–7 = RGB White
          id(uart_msg)[5] = 0xFF;
          id(uart_msg)[6] = 0xFF;
          id(uart_msg)[7] = 0xFF;

      - script.execute: uart_send
