script:
  - id: arm_handler
    parameters:
      arm_mode: string
      pin: string
      prefix: string
    then:
      - lambda: |-
          ESP_LOGI("arm_handler", "[ARM] Mode=%s, PIN=%s, Prefix=%s",
            arm_mode.c_str(), pin.c_str(), prefix.c_str());
